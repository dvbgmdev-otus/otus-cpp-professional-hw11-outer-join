{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run bulk_server (no debug)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/bulk_server",
            "args": ["9000", "3"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "preLaunchTask": "Project: Build"
        }
    ]
}
