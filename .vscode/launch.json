{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run join_server (no debug)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/join_server",
            "args": ["9000"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "preLaunchTask": "Project: Build"
        }
    ]
}
